import styled from "styled-components";
import { mainDarkBlue } from "../styles/colors";

const Header = styled.header`
  padding: 0 50px;
  background-color: ${mainDarkBlue};
`;

export default Header;
